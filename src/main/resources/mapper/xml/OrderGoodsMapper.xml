<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xiyifen.myshop.system.mapper.OrderGoodsMapper">

        <!-- 通用查询映射结果 -->
        <resultMap id="BaseResultMap" type="com.xiyifen.myshop.system.entity.OrderGoods">
                    <id column="id" property="id" />
                    <result column="order_id" property="orderId" />
                    <result column="goods_id" property="goodsId" />
                    <result column="goods_price" property="goodsPrice" />
                    <result column="goods_number" property="goodsNumber" />
                    <result column="goods_total_price" property="goodsTotalPrice" />
        </resultMap>

        <!-- 通用查询结果列 -->
        <sql id="Base_Column_List">
            id, order_id, goods_id, goods_price, goods_number, goods_total_price
        </sql>

</mapper>
